/*
CH-230-A
a1_p2.c
Chigozirim Margaret Arukwe
c.arukwe@jacobs-university.de
*/

#include <stdio.h>

int main()
{
    int result; /*The result of our calculation */
    result = (2 + 7) * 9 / 3;

    /* While the place holder '%d' was noted correctly,
    it does not show what variable holds the place.
    To correct it, add a comma and 'result' 
    after the end quotation marks. */

    printf("The result is %d\n", result);
    return 0;
}
